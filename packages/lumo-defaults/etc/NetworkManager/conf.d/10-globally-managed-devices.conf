# Lumo OS: NetworkManager must manage wired devices too.
# Debian's default (in /usr/lib) marks all non-wifi devices unmanaged,
# which leaves live/installer systems without wired networking.
# This /etc override takes precedence and re-enables management.
[keyfile]
unmanaged-devices=none
