# Lumo OS live bootstrap: run the (idempotent) live setup right before the
# display manager starts. The leading dash makes failures non-fatal.
# This drop-in is on the REAL unit name (sddm.service) so it always applies.
[Service]
ExecStartPre=-/usr/local/sbin/lumo-live-setup
